on:
  push:
    branches:
      - staging

name: Publish Website

jobs:
  web-deploy:
    name: 🚀 Deploy Website Every Commit
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get Latest Code
        uses: actions/checkout@v3
    
      - name: 📂 Sync Files
        uses: SamKirkland/web-deploy@v1
        with:
          target-server: 20.2.82.196
          remote-user: azureuser
          private-ssh-key: ${{ secrets.SSH_KEY }}
          source-path: .  # Change this to the directory you want to deploy
          destination-path: /var/www/html/wordpress/wp-content/themes/
